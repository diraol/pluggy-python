# coding: utf-8

"""
    Pluggy API

    Pluggy's main API to review data and execute connectors

    The version of the OpenAPI document: 1.0.0
    Contact: hello@pluggy.ai
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ConnectorIncident(BaseModel):
    """
    An incident affecting a connector right now, as published on the Pluggy status page. Only incidents active at this moment are listed: a scheduled maintenance appears once its window opens, not when it is announced, and disappears when the window closes.
    """ # noqa: E501
    id: UUID = Field(description="Status page identifier of the incident")
    title: StrictStr = Field(description="Short, customer-facing summary, for example 'XP Banking - Compras parceladas não sendo retornadas'")
    description: Optional[StrictStr] = Field(default=None, description="Longer explanation, when there is one")
    type: StrictStr = Field(description="What is broken, as opposed to how badly (severity) or how far along we are (state). Use it to group the same problem across institutions, and to decide which incidents are worth showing your own users: a SCHEDULED_MAINTENANCE and a TRANSACTIONS_MISSING both read as 'degraded' otherwise. OTHER means Pluggy has not classified it, not that nothing is wrong")
    product: StrictStr = Field(description="Which Pluggy product line the incident affects")
    kind: StrictStr = Field(description="Whether this is an unplanned incident or a planned maintenance window")
    severity: StrictStr = Field(description="How badly the institution is affected")
    state: StrictStr = Field(description="Where the incident is in its lifecycle. Resolved incidents are not listed")
    started_at: datetime = Field(description="When the incident began", alias="startedAt")
    updated_at: Optional[datetime] = Field(default=None, description="When the incident was last updated", alias="updatedAt")
    url: StrictStr = Field(description="Permalink to the incident on the status page, with its full timeline and postmortem. Safe to show to your own users")
    __properties: ClassVar[List[str]] = ["id", "title", "description", "type", "product", "kind", "severity", "state", "startedAt", "updatedAt", "url"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['CONNECTOR_UNAVAILABLE', 'CONNECTOR_DEGRADED', 'INSTITUTION_OUTAGE', 'SCHEDULED_MAINTENANCE', 'CONSENT_ERROR', 'CONNECTION_NOT_UPDATING', 'PARTIAL_SUCCESS', 'ACCOUNTS_MISSING', 'BALANCE_INCORRECT', 'TRANSACTIONS_MISSING', 'TRANSACTIONS_INCORRECT', 'TRANSACTIONS_INSTALLMENTS_ISSUE', 'INVESTMENTS_MISSING', 'INVESTMENTS_INCORRECT', 'IDENTITY_MISSING', 'HISTORICAL_DATA_MISSING', 'WEBHOOK_DELAY', 'PAYMENT_FAILURE', 'OTHER']):
            raise ValueError("must be one of enum values ('CONNECTOR_UNAVAILABLE', 'CONNECTOR_DEGRADED', 'INSTITUTION_OUTAGE', 'SCHEDULED_MAINTENANCE', 'CONSENT_ERROR', 'CONNECTION_NOT_UPDATING', 'PARTIAL_SUCCESS', 'ACCOUNTS_MISSING', 'BALANCE_INCORRECT', 'TRANSACTIONS_MISSING', 'TRANSACTIONS_INCORRECT', 'TRANSACTIONS_INSTALLMENTS_ISSUE', 'INVESTMENTS_MISSING', 'INVESTMENTS_INCORRECT', 'IDENTITY_MISSING', 'HISTORICAL_DATA_MISSING', 'WEBHOOK_DELAY', 'PAYMENT_FAILURE', 'OTHER')")
        return value

    @field_validator('product')
    def product_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['dados', 'pis', 'pis-agendado', 'pixauto', 'smart', 'infra']):
            raise ValueError("must be one of enum values ('dados', 'pis', 'pis-agendado', 'pixauto', 'smart', 'infra')")
        return value

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['INCIDENT', 'MAINTENANCE']):
            raise ValueError("must be one of enum values ('INCIDENT', 'MAINTENANCE')")
        return value

    @field_validator('severity')
    def severity_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['DEGRADED', 'PARTIAL_OUTAGE', 'MAJOR_OUTAGE', 'MAINTENANCE']):
            raise ValueError("must be one of enum values ('DEGRADED', 'PARTIAL_OUTAGE', 'MAJOR_OUTAGE', 'MAINTENANCE')")
        return value

    @field_validator('state')
    def state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['INVESTIGATING', 'IDENTIFIED', 'MONITORING', 'SCHEDULED']):
            raise ValueError("must be one of enum values ('INVESTIGATING', 'IDENTIFIED', 'MONITORING', 'SCHEDULED')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ConnectorIncident from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if description (nullable) is None
        # and model_fields_set contains the field
        if self.description is None and "description" in self.model_fields_set:
            _dict['description'] = None

        # set to None if updated_at (nullable) is None
        # and model_fields_set contains the field
        if self.updated_at is None and "updated_at" in self.model_fields_set:
            _dict['updatedAt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ConnectorIncident from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "title": obj.get("title"),
            "description": obj.get("description"),
            "type": obj.get("type"),
            "product": obj.get("product"),
            "kind": obj.get("kind"),
            "severity": obj.get("severity"),
            "state": obj.get("state"),
            "startedAt": obj.get("startedAt"),
            "updatedAt": obj.get("updatedAt"),
            "url": obj.get("url")
        })
        return _obj


